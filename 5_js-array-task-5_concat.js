let array1 = [1, 3];
let array2 = [5, 7];
//1. created two arrays of my choice.

console.log("Original array1: ", array1);
console.log("Original array2: ", array2);
console.log("Combined new array: ", array1.concat(array2));

//2 and 3. used the concat method to combine the two arrays into a new array and printed both the original arrays and the combined array using console.log().